name: Example
on: [push]
jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
    - uses: timburganec/checkout
#     - name: meh 
#       run: echo "${{secrets.GITHUB_TOKEN}}"
#     - name: boom
#       run: echo "${{ secrets.GITHUB_TOKEN }}"
