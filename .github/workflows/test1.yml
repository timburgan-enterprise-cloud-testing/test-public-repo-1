name: Example
on: [push]
jobs:
  test-mat:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package-type:
        - apk
        exclude:
        - package-type: 'apk'

    steps:
    - if: ${{matrix.package-type}} != ''
      name: Parse
      shell: bash
      env:
        TYPE: ${{matrix.package-type}}
      run: |
        echo TYPE is "'$TYPE'"
