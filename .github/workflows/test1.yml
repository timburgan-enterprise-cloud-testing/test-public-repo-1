on: push
jobs:
#   example_job:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: timburganec/checkout@v2
#     - name: meh 
#       run: echo "hi mum"
  example_job2:
    runs-on: windows-latest
    steps:
    - run: echo $HOME
    - run: |
        # echo "Hello"

        # Can the Azure CLI be installed here directly?
        # https://docs.microsoft.com/en-us/cli/azure/install-azure-cli-windows?tabs=azure-powershell
        # Invoke-WebRequest -Uri https://aka.ms/installazurecliwindows -OutFile .\AzureCLI.msi; Start-Process msiexec.exe -Wait -ArgumentList '/I AzureCLI.msi /quiet'; rm .\AzureCLI.msi

        # Use the CLI
        #az version
        
        ls
        az account set --subscription ${{ env.policySubscriptionId }}
        az storage blob sync -c ${{ env.policyStorageContainerName }} --account-name ${{ env.policyStorageName }} --account-key ${{ secrets.AZURE_BUILD_STORAGE_KEY }} -s "code/es/auxiliary"
