on:
  push:
    branches: [master]
    paths:
      - "anything-here/**"
      - "!anything-here/except-here/**"
  pull_request_target:
jobs:
  example_job:
    runs-on: ubuntu-latest
    steps:
    - uses: timburganec/checkout@v2
    - name: meh 
      run: echo "hi mum"
